<%= form_with(model: director) do |form| %>
  <% if director.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:</h2>

      <ul>
        <% director.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :name, :style => "color: #C7C7CC" %>
  <%= form.text_field :name, :class => "form-control", :style => "border: 1px solid gray; background-color: #2C2C2E; height: 7vh; color: #F2F2F7", :placeholder => "Jon Watts" %>

  <div class="artist_new_btn" style="text-align: end">
    <%= link_to "Cancelar", directors_path, :class => "btn btn-secondary" %>
    <%= form.submit :value => "Salvar", :class => "btn btn-danger my-4" %>
  </div>

<% end %>