<%= form_with(model: movie_genre) do |form| %>
  <% if movie_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie_genre.errors.count, "error") %> prohibited this movie genre from being saved:</h2>

      <ul>
        <% movie_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :name, :style => "color: #C7C7CC" %>
  <%= form.text_field :name, :class => "form-control", :style => "border: 1px solid gray; background-color: #2C2C2E; height: 7vh; color: #F2F2F7", :placeholder => "Ação" %>

  <div class="artist_new_btn" style="text-align: end">
    <%= link_to "Cancelar", movie_genres_path, :class => "btn btn-secondary" %>
    <%= form.submit :value => "Salvar", :class => "btn btn-danger my-4" %>
  </div>

<% end %>
